fastapi>=0.100.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
# PDF generation switched from weasyprint to playwright+chromium (smaller Docker image, modern CSS support)
playwright>=1.50.0
jinja2>=3.0.0
# XSS sanitizer: allowlist-based tag stripping (replaces fragile regex in server/sanitize.py)
bleach>=6.0.0
