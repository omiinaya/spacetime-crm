# Bandit configuration for spacetime-crm
# Generated by cyber-elf triage task

# Skip these tests entirely in production code
tests:
  - B101  # assert - False positive: test files excluded by path already, but B101 is fine in tests

# Exclude paths entirely
exclude_paths:
  - server/tests/
  - server/.venv/

# Severity/confidence thresholds
settings:
  # Don't report LOW severity issues (mostly assert warnings in tests)
  skip_low: false  # Still report LOW so we see them
