[package]
name = "spacetime-crm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { version = "=2.4.0" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
