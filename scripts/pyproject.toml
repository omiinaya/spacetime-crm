[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "spacetime-crm-scripts"
version = "0.1.0"
description = "Spacetime CRM utility scripts"
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 120
